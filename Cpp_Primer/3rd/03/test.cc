#include "String.h"
using user::String; 

#include <iostream>
using std::cin; 

#include <map>
using std::map;

int main()
{
    map<char, int> cnt; 
    String buf; 

    int worldCount; 
    while (cin >> buf) {
        ++worldCount; 

        for (int idx = 0; idx < buf.size(); ++idx) {
        }
    }


    return 0; 
}
